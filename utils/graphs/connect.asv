function list = connect(v,w,beta)

n = size(v,1); m = size(v,2); k = size(v,3);
ind = gen_indices([n m k]);
w = get_frus(v,w);
A = get_A(w);
G = graph(ind);

for i = 1:n
for j = 1:m
for l = 1:k
   
    
    
end
end
end

end