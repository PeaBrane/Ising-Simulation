function fn = get_fn(list,iter)

[sz,list] = collapse(list);
[


ll = length(list);
fn = blanks(0);

for i = ll:-1:1
    var = list(i);
    fn = strcat(fn,num2str(var),'-');
end

fn = fn(1:end-1);
fn = strcat(fn,'.mat');

end